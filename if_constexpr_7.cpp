//modern c++ examples

template<typename H, typename... Ts>
void f(H &&h, Ts &&...ps) {
	g(h);
	if constexpr (sizeof...(ps) != 0) {
		// There are more parameters to handle.
		f(ps...);
	}
}
//----------------------------------------------------------------------------------------------------
//----------------------------------------------------------------------------------------------------

